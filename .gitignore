# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Whisper assets from scripts/fetch-whisper-assets.ps1 (large; fetch locally / in CI)
src-tauri/binaries/*.dll
src-tauri/resources/models/ggml-base.bin

# llama.cpp Rewrite sidecar from scripts/fetch-llama-assets.ps1 (large; no GGUF in repo)
src-tauri/binaries/llama-cli-x86_64-pc-windows-msvc.exe
# GGUF models stay out of git / NSIS — download-on-demand or ISSUEBRIDGE_REWRITE_GGUF
*.gguf

# Local scratch (security reports, experiments — never commit)
.scratch/

# Security-audit working files (never commit findings)
security-audit-report.md
security-audit-report.local.md
security-audit-user.txt
security-audit-user.local.txt
security-audit-cli.log
security-audit-session.md

